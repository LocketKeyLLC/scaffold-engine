-r requirements.txt
pytest==9.1.1  # §17.585 — CVE-2025-71176
pytest-timeout==2.4.0
pytest-asyncio==1.4.0
# §17.553 — coverage measurement (make coverage). Dev-only; pruned from the
# prod runtime image by scripts/_prune_dev_deps.py. NOT in pytest addopts
# (cloud ci-smoke runs requirements-ci.txt without it) — opt-in per-target.
pytest-cov==7.1.0
# X.21 — bench_pipeline + bench_rag + bench_embed system metrics
psutil==7.2.2
# §17.97 — CVE scan against pinned deps; surfaced via `make audit`.
# Pulls PyPA's Advisory DB at scan time; no daemon, no network access
# at install time. Local-only dev tool — NOT in requirements.txt.
pip-audit==2.10.1
# §17.1057 — dev-image tooling (pruned from prod by scripts/_prune_dev_deps.py).
# mutation testing over the gate modules (make mutate)
mutmut==3.8.0
# memory profiler for the OOM lane (make profile-mem)
memray==1.20.0
# CPU profiler for a test or a script (make profile-cpu)
pyinstrument==5.1.3
# module-boundary contracts, parses source via grimp (make lint-imports)
import-linter==2.15
# dead-code report with whitelist (make dead-code)
vulture==2.16
