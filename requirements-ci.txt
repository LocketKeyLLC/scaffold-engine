# Minimal dependencies for Tier 1 (ci-smoke) cloud CI tests.
# Must resolve full app import chain for unittest.mock.patch targets.
#
# Pinned to match requirements.txt / requirements-dev.txt production versions
# (#82) so CI never drifts from what the container actually runs.
fastapi==0.141.1
starlette==1.6.0  # §17.584 — CVE-2026-48710 + PYSEC-2026-248/249 (see requirements.txt)
uvicorn[standard]==0.52.4
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
# §17.772 — Model Context Protocol SDK (both sides: engine-as-server + MCP DAG
# nodes). Kept in lockstep with requirements.txt; mcp 2.0 ships its own
# httpx2/httpcore2 transport stack alongside our httpx 0.28.1.
mcp==2.0.0
mcp-types==2.0.0
sse-starlette==3.4.8
httpx2==2.12.0
httpcore2==2.12.0
PyJWT==2.13.0
truststore==0.10.4
pymilvus==3.0.1
setuptools==83.0.0  # §17.585
json_repair==0.63.3
requests==2.34.2
structlog==26.1.0
prometheus-client==0.26.0
redis==8.1.0
trafilatura==2.1.0  # HELD at 2.1.x (mirror requirements.txt) — 2.2.0 heading-loss regression, audit M12
pypdf==6.16.1  # §17.585, §17.724 GHSA bump
pdfplumber==0.11.10  # §17.585 — pulls pdfminer.six 20260107 (CVE-2025-64512/70559)
apscheduler==3.11.3
tzlocal==5.4.4
jinja2==3.1.6  # §17.585
python-multipart==0.0.32  # §17.585
markdown-it-py==4.2.0
pytest==9.1.1  # §17.585 — CVE-2025-71176
pytest-timeout==2.4.0
pytest-asyncio==1.4.0
jsonschema==4.26.0
