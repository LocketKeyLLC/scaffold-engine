apscheduler==3.10.4
asyncpg==0.30.0
fastapi==0.115.6
# §17.143 — JSON Schema validation for spec-capture (engineering-design track).
# Pure-Python, validates app/sim/spec_schema.json against incoming spec
# payloads. No optional deps pulled in.
jsonschema==4.26.0
httpx==0.28.1
json_repair==0.58.7
pydantic==2.10.3
pydantic-settings==2.7.0
pymilvus==2.5.18
redis==7.4.0
requests==2.33.1
# sentence-transformers 5.x is softly-breaking vs 4.x; we only use
# CrossEncoder(...).predict(...) which is API-stable (#86).
sentence-transformers==5.3.0
setuptools==71.1.0
sqlalchemy[asyncio]==2.0.36
structlog==25.5.0
trafilatura==2.0.0
tzlocal==5.2
uvicorn[standard]==0.34.0
psycopg2-binary==2.9.9

# --- PDF ingestion (4.5b) ---
pypdf==5.1.0
pdfplumber==0.11.4
python-multipart==0.0.17

# --- OpenAPI ingestion (4.5d) ---
openapi-spec-validator==0.7.1
jinja2==3.1.4
prance==25.4.8.0

# --- Observability (X.26) ---
# Prometheus metrics surface; always loaded so `/metrics` is on by default.
prometheus-client==0.21.1
# OpenTelemetry — strictly opt-in (gated by settings.otel_enabled).
# Pinned to the 1.29.0 line so the API/SDK/exporter/instrumentation
# packages stay compatible. Importing these at process start is cheap
# (~50ms); the SDK only initializes when init_tracing() actually runs.
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-httpx==0.50b0
opentelemetry-instrumentation-asyncpg==0.50b0
