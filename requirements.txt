apscheduler==3.11.3
asyncpg==0.31.0
fastapi==0.141.1
# §17.584 — pin starlette explicitly (fastapi 0.139 allows >=0.46). 1.3.1 clears
# CVE-2026-48710 "BadHost" host-header auth-bypass (fix 1.0.1) + PYSEC-2026-248/
# 249 (fix 1.3.0/1.3.1). fastapi bumped 0.115.6→0.139.0 to permit starlette 1.x.
starlette==1.6.0
# §17.143 — JSON Schema validation for spec-capture (engineering-design track).
# Pure-Python, validates app/sim/spec_schema.json against incoming spec
# payloads. No optional deps pulled in.
jsonschema==4.26.0
httpx==0.28.1
# §17.772 — Model Context Protocol. Powers BOTH sides of the integration:
# (a) exposing the engine as an MCP server (streamable-HTTP mount at /mcp +
# a stdio entrypoint), and (b) consuming external MCP servers' tools as DAG
# nodes (tool='MCP'). mcp 2.0 ships its own httpx2/httpcore2 stack (coexists
# with our httpx 0.28.1 — different top-level package names), plus
# sse-starlette for the HTTP transport and PyJWT for auth helpers. The SDK
# and its wire-transport deps must move in lockstep, so they are pinned
# together (same discipline as the opentelemetry block below).
mcp==2.0.0
mcp-types==2.0.0
sse-starlette==3.4.8
httpx2==2.12.0
httpcore2==2.12.0
PyJWT==2.13.0
truststore==0.10.4
json_repair==0.63.3
pydantic==2.13.4
pydantic-settings==2.15.0
pymilvus==3.0.1
redis==8.1.0
requests==2.34.2
# sentence-transformers 5.x is softly-breaking vs 4.x; we only use
# CrossEncoder(...).predict(...) which is API-stable (#86).
sentence-transformers==6.0.0
setuptools==83.0.0  # §17.585 — was 71.1.0 (PYSEC-2025-49); also bumped in Dockerfile
sqlalchemy[asyncio]==2.0.52
structlog==26.1.0
trafilatura==2.1.0  # HELD: 2.2.0 drops <h1> headings from extraction (audit M12) — see .github/dependabot.yml ignore
tzlocal==5.4.4
uvicorn[standard]==0.52.4
psycopg2-binary==2.9.12

# --- PDF ingestion (4.5b) ---
# §17.585 — pypdf 5.1.0→6.14.2 (major; clears ~30 GHSAs). Code uses only the
# stable modern API (PdfReader/.pages/.extract_text() at research_extractors.py
# + forum_ingest.py), unaffected by the 6.0 removals.
# §17.724 — 6.14.2→6.15.0 (GHSA-fwg2-594c-jp42 + GHSA-fp3f-mc75-235c).
pypdf==6.16.1
pdfplumber==0.11.10  # §17.585 — was 0.11.4; pulls pdfminer.six 20260107 (CVE-2025-64512/70559)
python-multipart==0.0.32  # §17.585 — was 0.0.17 (GHSA); fastapi form/file parsing
# §17.458 — server-side markdown rendering for the web UI's compiled_output.
# Already present transitively via rich; pinned explicitly now that app code is
# a direct consumer. Used with html=False (raw HTML escaped) — see app/web/routes.py.
markdown-it-py==4.2.0

# --- OpenAPI ingestion (4.5d) ---
openapi-spec-validator==0.9.0
jinja2==3.1.6  # §17.585 — was 3.1.4 (GHSA-q2x7-8rv6-6q7h)
prance==26.7.19.0

# --- Observability (X.26) ---
# Prometheus metrics surface; always loaded so `/metrics` is on by default.
prometheus-client==0.26.0
# OpenTelemetry — strictly opt-in (gated by settings.otel_enabled).
# §17.584 — bumped 1.29.0/0.50b0 → 1.43.0/0.64b0 in lockstep with the fastapi
# 0.139 upgrade: instrumentation-fastapi 0.50b0 crashes on fastapi 0.139's new
# `_IncludedRouter` route type (AttributeError: no attribute 'path') on every
# request when otel_enabled. API/SDK/exporter (1.43.0) + instrumentation
# (0.64b0) must move together. Importing at process start stays cheap; the SDK
# only initializes when init_tracing() actually runs.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-instrumentation-asyncpg==0.65b0
